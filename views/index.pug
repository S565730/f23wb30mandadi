extends layout
 
block content
  h1= title
  p Welcome to page
 
  button(type="button", class="btn btn-secondary" onClick="doBump()") Press  me
  div(id="faster") Increase the speed
  script.
    console.log("running on http://localhost:3000/")
    var mph = 20;
    var change = 1;

    function goFaster() {
    mph += change;
    change += 1;

    document.getElementById("faster").innerHTML = "MPH " + mph;
    }
